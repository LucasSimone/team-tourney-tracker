services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - ./db:/db
    restart: unless-stopped
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    restart: unless-stopped
